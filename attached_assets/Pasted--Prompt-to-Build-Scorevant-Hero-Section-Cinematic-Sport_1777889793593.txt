**Prompt to Build “Scorevant” Hero Section – Cinematic, Sport-Driven Landing Experience**

Create a **single-page hero section** for **Scorevant**, a modern scoreboard platform for racket sports, using **React + Vite + Tailwind CSS + TypeScript with shadcn/ui**. The hero should feel **cinematic, energetic, and sport-focused**, while maintaining a clean, minimal aesthetic inspired by the Scorevant logo (bold typography, black + gold contrast, dynamic motion).

---

### 🎥 Background Video (Sport Energy Focus)

* Fullscreen `<video>` element:

  * `autoPlay`, `loop`, `muted`, `playsInline`
  * Positioned: `absolute inset-0 w-full h-full object-cover z-0`
* Use a **high-energy racket sports clip** (badminton smash / tennis rally / shuttle motion blur)
* The video should provide **all visual depth** — no overlays, gradients, or blobs

---

### 🎨 Theme & Branding (Scorevant Identity)

* **Primary Colors**:

  * Black (#000000) → dominant background tone
  * Gold/Yellow (#F2C94C) → accents, highlights
  * White/Grey → text contrast
* Typography:

  * Headings: bold, blocky, slightly pixel-inspired (logo-inspired)
  * Body: clean sans-serif (Inter)
* Visual vibe:

  * Competitive, sharp, real-time
  * Minimal but powerful

---

### 🧭 Navigation Bar (Glassmorphic + Sporty)

* `relative z-10 flex justify-between px-8 py-6 max-w-7xl mx-auto`
* Logo: **"Scorevant"**

  * Bold display font, gold + white styling (split-tone like logo)
  * Slight outline/glow effect
* Nav Links (md:flex, hidden on mobile):

  * Home (active), Matches, Sports, Leaderboard, About
  * `text-sm text-muted-foreground hover:text-foreground`
* CTA Button:

  * **"Start Match"**
  * `.liquid-glass rounded-full px-6 py-2.5`
  * Subtle hover scale

---

### 🏆 Hero Section (Core Experience)

* Layout:

  * `relative z-10 flex flex-col items-center justify-center text-center px-6 pt-32 pb-40`
* **Headline (H1)**:

  * Text:
    **"Where every rally becomes a story."**
  * Style:

    * `text-5xl sm:text-7xl md:text-8xl`
    * Tight tracking, bold presence
    * Highlight key words like *"rally"* or *"story"* in gold
* **Subtext**:

  * `"Track every point, every serve, and every victory in real-time. Scorevant brings modern, lightning-fast scorekeeping to badminton, tennis, table tennis, and more."`
  * `text-muted-foreground text-base sm:text-lg max-w-2xl mt-8`
* **Primary CTA**:

  * `"Start Live Match"`
  * `.liquid-glass px-14 py-5 rounded-full mt-12`
* **Secondary CTA (optional)**:

  * `"Watch Demo"` (ghost style)

---

### ✨ Motion & Animations

* Use subtle entry animations:

  * H1 → `animate-fade-rise`
  * Subtext → `animate-fade-rise-delay`
  * CTA → `animate-fade-rise-delay-2`
* Keep animations smooth and fast (sports feel, not slow luxury)

---

### 🧊 Glassmorphism (Liquid Glass UI)

Use the provided `.liquid-glass` class:

* Applied to:

  * Navbar CTA
  * Hero buttons
* Should feel like **frosted glass over motion**, not heavy blur

---

### ⚡ Scorevant-Specific Enhancements

* Add a **floating mini-scoreboard preview** (optional):

  * Example:

    * Player A: 18
    * Player B: 20
  * Styled in gold highlight for leading player
* Subtle **score tick animation** idea (future expansion)
* Use **sports motion cues** (like shuttle trajectory arcs) very lightly if needed

---

### 📱 Responsiveness

* Mobile-first design
* Centered layout always
* Nav collapses into hamburger menu
* Maintain readability over video

---

### 🎯 End Goal

Build a hero section that feels like:

* A **live sports moment frozen in time**
* A **premium yet casual sports product**
* Instantly communicates: *“This is where matches come alive.”*

No clutter. No distractions. Just motion, typography, and impact.
